sudo: required

language: ruby

# env:
#   global:
#     - DOCKER_COMPOSE_VERSION=2

services:
  - docker

before_install:
  - docker-compose build
  - docker-compose up -d

script:
  - ./test.sh

cache:
  directories:
    - docker
